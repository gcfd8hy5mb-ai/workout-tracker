/* PRISM Pro Advanced Analytics beta. Read-only analysis of saved workout history. */
function prismAnalyticsValidSets(session){return (session.exercises||[]).flatMap(ex=>(ex.sets||[]).filter(s=>Number(s.weight)>0&&Number(s.reps)>0).map(set=>({ex,set})));}
function prismAnalyticsPeriodDays(days){const end=new Date(),start=new Date();start.setHours(0,0,0,0);start.setDate(start.getDate()-days+1);return (workoutHistory||[]).filter(s=>{const d=new Date(s.date);return !Number.isNaN(d.getTime())&&d>=start&&d<=end;});}
function prismAnalyticsTotals(sessions){const sets=sessions.flatMap(prismAnalyticsValidSets);return {workouts:sessions.length,sets:sets.length,volume:Math.round(sets.reduce((n,x)=>n+Number(x.set.weight)*Number(x.set.reps),0))};}
function prismAnalyticsMuscles(sessions){const out=new Map();for(const s of sessions)for(const {ex,set} of prismAnalyticsValidSets(s)){const muscle=getExercise(ex.id)?.muscle||'Other',volume=Number(set.weight)*Number(set.reps),old=out.get(muscle)||{muscle,sets:0,volume:0};old.sets++;old.volume+=volume;out.set(muscle,old);}return [...out.values()].sort((a,b)=>b.volume-a.volume);}
function prismAnalyticsExerciseSeries(id){const rows=[];for(const s of (workoutHistory||[]).slice().reverse()){const ex=(s.exercises||[]).find(x=>x.id===id);if(!ex)continue;const valid=(ex.sets||[]).filter(x=>Number(x.weight)>0&&Number(x.reps)>0);if(!valid.length)continue;const best=valid.reduce((a,b)=>Number(b.weight)*(1+Number(b.reps)/30)>Number(a.weight)*(1+Number(a.reps)/30)?b:a,valid[0]);rows.push({day:sessionDay(s),weight:Number(best.weight),reps:Number(best.reps),score:Number(best.weight)*(1+Number(best.reps)/30)});}return rows;}
function prismAnalyticsExerciseLeaders(){const ids=[...new Set((workoutHistory||[]).flatMap(s=>(s.exercises||[]).map(e=>e.id)))],rows=[];for(const id of ids){const series=prismAnalyticsExerciseSeries(id);if(series.length<2)continue;const first=series[0],last=series[series.length-1],change=first.score?((last.score-first.score)/first.score)*100:0;rows.push({id,name:getExercise(id)?.name||id,change,sessions:series.length,last});}return rows.sort((a,b)=>b.change-a.change);}
function prismAnalyticsBuild(){const current=prismAnalyticsPeriodDays(30),previousStart=new Date();previousStart.setHours(0,0,0,0);previousStart.setDate(previousStart.getDate()-59);const previousEnd=new Date();previousEnd.setHours(23,59,59,999);previousEnd.setDate(previousEnd.getDate()-30);const previous=(workoutHistory||[]).filter(s=>{const d=new Date(s.date);return !Number.isNaN(d.getTime())&&d>=previousStart&&d<=previousEnd;});return {current:prismAnalyticsTotals(current),previous:prismAnalyticsTotals(previous),muscles:prismAnalyticsMuscles(current),leaders:prismAnalyticsExerciseLeaders(),hasComparison:previous.length>0};}
function prismAnalyticsDelta(now,before,label){if(!before)return `No previous 30-day ${label} baseline yet.`;const pct=(now-before)/before*100;return `${pct>=0?'+':''}${pct.toFixed(0)}% vs previous 30 days`;}
function prismAnalyticsRender(){const host=document.getElementById('prismAdvancedAnalytics');if(!host)return;if(typeof canAccessFeature==='function'&&!canAccessFeature('advanced_analytics')){host.innerHTML=`<div class="card"><span class="journey-eyebrow">PRISM PRO ✦</span><h3>Advanced Analytics</h3><p class="small">Go beyond totals with 30-day comparisons, muscle-group workload and exercise progression.</p><button type="button" onclick="showProPreview('advanced_analytics')">Preview Advanced Analytics</button></div>`;return;}const data=prismAnalyticsBuild(),top=data.leaders[0],muscles=data.muscles.slice(0,5);host.innerHTML=`<div class="card"><span class="journey-eyebrow">PRISM PRO ANALYTICS</span><h3>Last 30 days</h3><div class="stat-grid"><div class="stat-card"><div class="stat-number">${data.current.workouts}</div><div class="stat-label">Workouts</div></div><div class="stat-card"><div class="stat-number">${data.current.sets}</div><div class="stat-label">Working sets</div></div><div class="stat-card"><div class="stat-number">${data.current.volume.toLocaleString()}</div><div class="stat-label">Volume (lb)</div></div><div class="stat-card"><div class="stat-number">${data.hasComparison?prismAnalyticsDelta(data.current.volume,data.previous.volume,'volume').split(' ')[0]:'—'}</div><div class="stat-label">Volume trend</div></div></div><p class="small">${escapeHTML(data.hasComparison?prismAnalyticsDelta(data.current.volume,data.previous.volume,'volume'):'Complete another 30-day period to unlock a direct volume comparison.')}</p></div><div class="card"><h3>Muscle workload</h3>${muscles.length?muscles.map(x=>`<div class="journey-summary"><span>${escapeHTML(x.muscle)} · ${x.sets} sets</span><strong>${Math.round(x.volume).toLocaleString()} lb</strong></div>`).join(''):'<p class="small">Log workouts to build your muscle workload view.</p>'}</div><div class="card"><h3>Exercise progression</h3>${data.leaders.length?data.leaders.slice(0,5).map(x=>`<div class="journey-summary"><span>${escapeHTML(x.name)} · ${x.sessions} sessions</span><strong>${x.change>=0?'+':''}${x.change.toFixed(1)}%</strong></div>`).join(''):'<p class="small">Repeat exercises across multiple workouts to unlock progression trends.</p>'}${top?`<p class="small">Strongest measured trend: ${escapeHTML(top.name)} at ${top.change>=0?'+':''}${top.change.toFixed(1)}% estimated performance across logged sessions.</p>`:''}</div>`;}
function prismAnalyticsMount(){const screen=document.getElementById('overallProgressScreen');if(!screen)return;let host=document.getElementById('prismAdvancedAnalytics');if(!host){host=document.createElement('div');host.id='prismAdvancedAnalytics';const weekly=document.getElementById('weeklySummary');weekly?.insertAdjacentElement('afterend',host);}prismAnalyticsRender();}
window.prismAnalyticsBuild=prismAnalyticsBuild;window.prismAnalyticsRender=prismAnalyticsRender;window.prismAnalyticsMount=prismAnalyticsMount;
if(typeof showOverallProgress==='function'&&!showOverallProgress.__prismAnalytics){const original=showOverallProgress;showOverallProgress=function(){original();prismAnalyticsMount();};showOverallProgress.__prismAnalytics=true;}